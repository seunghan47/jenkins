pipeline {
    agent any
    stages {
        stage('Install Depedencies') {
            steps {
                echo "install dependencies"
                sh 'sleep 10s'
            }
        }
    }
}